Run: d8d467ac-276f-497e-84f6-40c902bd7fb3
Story: fix-005
Status: done

Completed story results:
- Fixed reflected XSS in the `/search` handler in `src/server.js` by treating `q` as user-controlled text, requiring a string value, and HTML-encoding it with `escape-html` before interpolating it into the HTML response.
- Kept the change minimal and contextual: the route still returns HTML, but user input is now rendered as inert text instead of executable markup.
- Added a regression test in `test/web-security.test.js` that sends a `<script>alert("owned")</script>` payload to `/search` and verifies the response body contains the encoded form (`&lt;script&gt;...`) and no raw `<script>` tag.
- Verified `npm run build` passes.
- Verified `npm test` passes.

Patterns:
- For reflected XSS, encode untrusted data for the output context at the final render point rather than attempting ad hoc blacklist filtering.
- Preserve intended response format while ensuring user-controlled values are treated strictly as text.
- Regression tests for XSS should use realistic script payloads and assert both the encoded output and the absence of executable markup.
