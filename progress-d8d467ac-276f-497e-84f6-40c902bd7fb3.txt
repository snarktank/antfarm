Run: d8d467ac-276f-497e-84f6-40c902bd7fb3
Story: fix-006
Status: done

Completed story results:
- Removed repository-tracked secrets from `src/config.js` by replacing hardcoded API/admin/JWT values with environment-backed `getConfig()` and `validateConfig()` helpers that require `API_KEY`, `ADMIN_PASSWORD`, and `JWT_SECRET` at runtime.
- Replaced the unsafe `/debug` handler in `src/server.js` so it now returns a 404 error instead of dumping `process.env` and runtime details.
- Updated `src/write-log.sh` to redact JWT-derived log output and avoid writing secret material to `app.log`.
- Replaced secret-like placeholders in `.env.example` with non-live rotation placeholders and documented the required rotation work in `docs/secret-rotation.md`.
- Added regression coverage in `test/web-security.test.js` for environment-based config loading, the locked-down `/debug` endpoint, and log redaction.
- Verified `npm run build` passes.
- Verified `npm test` passes.

Patterns:
- Remove hardcoded secrets entirely from tracked source and fail fast when required secret environment variables are missing.
- Treat debug/diagnostic endpoints as high-risk surfaces; return a generic not-found response when runtime internals should never be exposed.
- Redact secrets before logging and assert on the rendered log output in regression tests so raw token values cannot leak back in.
- Document secret rotation separately from source code and keep checked-in environment templates limited to placeholders only.
