# Doc-Revision Workflow Progress

## Story 001 ✅ (Completed: 2026-02-16 17:20 EST)
**Title:** Create workflow directory structure and workflow.yml skeleton

**What was done:**
- Created ~/clawd/antfarm/workflows/doc-revision/ directory structure
- Created agents/ subdirectory with 5 agent folders: reader, analyzer, revisor, qa, cleaner
- Created SOUL.md files for each agent with role-specific personality and working style
- Created workflow.yml skeleton with:
  - Workflow metadata (id, name, version, description)
  - 5 agent definitions (reader, analyzer, revisor, qa, cleaner)
  - Empty steps array (to be populated in future stories)
- Wrote comprehensive test suite: tests/doc-revision-workflow-structure.test.ts
- All tests pass ✅
- Typecheck/build passes ✅

**Files created:**
- workflows/doc-revision/workflow.yml
- workflows/doc-revision/agents/reader/SOUL.md
- workflows/doc-revision/agents/analyzer/SOUL.md
- workflows/doc-revision/agents/revisor/SOUL.md
- workflows/doc-revision/agents/qa/SOUL.md
- workflows/doc-revision/agents/cleaner/SOUL.md
- tests/doc-revision-workflow-structure.test.ts

**Commit:** aa67f15 - feat: story-001 - Create workflow directory structure and workflow.yml skeleton

## Story 002 ✅ (Completed: 2026-02-16 17:27 EST)
**Title:** Implement reader agent for Markdown extraction

**What was done:**
- Created markdown-extractor.ts module in src/lib/doc-revision/
- Implemented extractMarkdownComments() function that:
  - Extracts all %% comment %% blocks (single-line and multi-line)
  - Captures 2 lines of context before and after each comment
  - Returns structured output: {commentText, lineNumber, contextBefore, contextAfter}
  - Handles edge cases: comments at file start/end, nested %%, empty comments
- Updated reader agent SOUL.md with usage instructions
- Wrote comprehensive test suite with 15 test cases
- All tests pass ✅
- Typecheck/build passes ✅

**Files created/modified:**
- src/lib/doc-revision/markdown-extractor.ts (new)
- tests/doc-revision-markdown-extractor.test.ts (new)
- workflows/doc-revision/agents/reader/SOUL.md (updated)

**Commit:** 6191f5d - feat: story-002 - Implement reader agent for Markdown extraction

## Story 003 ✅ (Completed: 2026-02-16 17:42 EST - RETRY)
**Title:** Implement reader agent for Word extraction with python-docx

**What was done (retry to fix track changes extraction):**
- Fixed Python script (extract-word-comments.py) to properly extract track changes:
  - Added extract_text_from_element() helper function
  - Implemented extraction of w:ins (insertions) with author, date, and text
  - Implemented extraction of w:del (deletions) with author, date, and text
  - Fixed delText element parsing (deletions use w:delText, not w:t)
  - Track changes now correctly linked to paragraph indices
- Created test fixture .docx file with real track changes:
  - Created create-test-docx-with-changes.py to generate test files via XML manipulation
  - Generated tests/fixtures/test-track-changes.docx with 2 insertions and 2 deletions
  - Each change has author, date, and text content
- Updated TypeScript wrapper (word-extractor.ts):
  - Fixed ES module compatibility (__dirname not available, use import.meta.url)
  - Fixed Python script path to reference src/ directory (scripts don't get compiled)
- Updated test suite with real extraction verification:
  - Tests now verify against real fixture file
  - Validates insertions are extracted with correct metadata
  - Validates deletions are extracted with correct metadata
  - Verifies author/date metadata is present and correctly formatted
  - Confirms paragraph indices are valid
  - All 13 tests pass ✅
- Typecheck/build passes ✅

**Files created/modified:**
- src/lib/doc-revision/extract-word-comments.py (fixed track changes extraction)
- src/lib/doc-revision/word-extractor.ts (fixed ES module compatibility)
- src/lib/doc-revision/create-test-docx-with-changes.py (new - creates test fixtures)
- src/lib/doc-revision/create-test-docx.py (new - helper script)
- tests/fixtures/test-track-changes.docx (new - test fixture)
- tests/doc-revision-word-extractor.test.ts (updated with real extraction tests)

**Technical notes:**
- Track changes use w:ins and w:del XML elements
- Deleted text is stored in w:delText elements (not w:t like normal text)
- Python scripts referenced from src/ directory (not copied to dist/)
- ES modules require import.meta.url instead of __dirname
- Test fixture created programmatically using python-docx XML manipulation

**Commit:** df4e635 - feat: story-003 - Implement track changes extraction for Word documents

## Story 004 ✅ (Completed: 2026-02-16 17:52 EST)
**Title:** Implement file type auto-detection logic

**What was done:**
- Created document-router.ts module in src/lib/doc-revision/
- Implemented extractDocumentComments() function that:
  - Auto-detects file type from extension (.md/.markdown vs .docx)
  - Routes to appropriate extraction method (markdown or word)
  - Returns unified output format regardless of input type
  - Returns descriptive errors for unsupported file types
  - Logs which extraction method was used (visible in console)
- Implemented detectFileType() helper function:
  - Normalizes extension (handles with/without dot)
  - Returns 'markdown' for .md/.markdown
  - Returns 'word' for .docx
  - Returns null for unsupported types (.doc, .txt, etc.)
- Created UnifiedComment interface:
  - text: comment/change text
  - lineOrParagraphNumber: location in document
  - contextBefore/contextAfter: arrays of context strings
  - author/date: optional metadata (from Word)
  - type: 'comment' | 'insertion' | 'deletion'
- Wrote comprehensive test suite with 13 test cases covering:
  - File type detection for .md, .markdown, .docx
  - Error handling for unsupported types (.txt, .doc)
  - Error handling for non-existent files
  - Markdown extraction via router
  - Word extraction via router
  - Unified output format validation
  - Logging verification
- All 13 tests pass ✅
- Typecheck/build passes ✅

**Files created:**
- src/lib/doc-revision/document-router.ts (new)
- tests/doc-revision-document-router.test.ts (new)

**Technical notes:**
- Router provides single entry point for both markdown and Word extraction
- Unified interface makes it easy for downstream agents to work with either format
- Console logging helps with debugging and transparency
- Error messages are descriptive and actionable

**Acceptance Criteria Met:**
1. ✅ Reader agent checks file extension (.md/.markdown vs .docx)
2. ✅ Routes to appropriate extraction method
3. ✅ Returns error for unsupported file types
4. ✅ Logs which extraction method was used
5. ✅ Unified output format regardless of input type
6. ✅ Typecheck passes

**Commit:** 76ec6db - feat: story-004 - Implement file type auto-detection logic

## Codebase Patterns

### Directory Structure
- Workflows live in `workflows/<workflow-name>/`
- Each workflow has an `agents/` subdirectory with one folder per agent
- Each agent folder contains at least a SOUL.md file

### Agent SOUL.md Format
SOUL.md files follow this structure:
```markdown
# <Agent Name> - Soul

You're a [role description]. [Core job description]

## Personality
[Character traits and approach]

## How You Work
[Concrete work process, typically bullet points]

## Communication Style
[How the agent communicates findings/results]

## What You Care About
[Core values and priorities]
```

### Workflow.yml Structure
Required top-level fields:
- `id`: kebab-case workflow identifier
- `name`: Human-readable workflow name
- `version`: Numeric version
- `description`: Multi-line description of what the workflow does
- `agents`: Array of agent definitions
- `steps`: Array of workflow steps (can be empty for skeleton)

Agent definitions require:
- `id`: Agent identifier
- `name`: Human-readable agent name
- `role`: Agent role type (coding, verification, analysis, etc.)
- `description`: What the agent does
- `workspace`: Object with `baseDir` and `files` mapping

### Testing Patterns
- Tests live in `tests/` directory
- Use Node.js native test runner (`node:test`)
- Import assertions from `node:assert/strict`
- Test file names follow pattern: `<feature>.test.ts`
- Test structure: describe blocks with it tests
- Use yaml package to parse workflow.yml files in tests
- Import from `dist/` not `src/` (compiled output)

### Workflow Library Code
- Workflow-specific TypeScript code goes in `src/lib/<workflow-name>/`
- This ensures it's compiled by the build process (tsconfig includes `src/**/*.ts`)
- Tests import from `dist/lib/<workflow-name>/*.js`
- Agent SOUL.md files document the API and usage patterns

### Auto-Detection Router Pattern
- Use a router module to auto-detect input format and route to appropriate handler
- Router should check file extension and validate file existence
- Return unified output interface regardless of input format
- Include descriptive error messages for unsupported formats
- Log which extraction method was used (aids debugging)
- Example: document-router.ts detects markdown vs Word and unifies output

## Story 005 ✅ (Completed: 2026-02-16 17:57 EST)
**Title:** Implement analyzer agent feedback categorization

**What was done:**
- Created analyzer.ts module in src/lib/doc-revision/
- Implemented categorizeFeedback() function that:
  - Takes UnifiedComment[] as input (output from reader agent)
  - Categorizes each comment into one of 4 categories: factual_error, missing_info, tone, structure
  - Uses keyword-based pattern matching for categorization
  - Assigns priority (high/medium/low) based on category and severity indicators
  - Returns CategorizedFeedback[] with original comment + category + priority + reasoning
  - Defaults to factual_error (most conservative) for truly ambiguous feedback
- Updated analyzer agent SOUL.md with:
  - Usage instructions and code examples
  - Category definitions and keyword lists
  - Priority rules and defaults
  - Ambiguity handling approach
- Wrote comprehensive test suite with 18 test cases covering:
  - Category detection for all 4 types (factual_error, missing_info, tone, structure)
  - Priority assignment (defaults + explicit overrides)
  - Output structure validation
  - Ambiguity handling (defaults to factual_error)
  - Multiple comments and empty arrays
  - Real-world scenarios (track changes, markdown comments with context)
- All 18 analyzer tests pass ✅
- All 73 total tests pass ✅
- Typecheck/build passes ✅

**Files created/modified:**
- src/lib/doc-revision/analyzer.ts (new)
- tests/doc-revision-analyzer.test.ts (new)
- workflows/doc-revision/agents/analyzer/SOUL.md (updated)

**Technical notes:**
- Keyword-based categorization is simple but effective for common feedback patterns
- Priority can be overridden by explicit indicators (critical, urgent, minor, nit, etc.)
- Category defaults: factual_error=high, missing_info=medium, tone=low, structure=medium
- Reasoning field provides transparency about why each categorization was made
- Works with both markdown comments and Word track changes (unified input format)

**Acceptance Criteria Met:**
1. ✅ Analyzer agent SOUL.md defines categorization approach
2. ✅ Takes reader output (UnifiedComment[]) as input
3. ✅ Categorizes each comment/change into one of 4 categories
4. ✅ Assigns priority (high/medium/low) based on category and severity
5. ✅ Returns structured output: original comment + category + priority + reasoning
6. ✅ Handles ambiguous feedback by defaulting to factual_error (most conservative)
7. ✅ Typecheck passes

**Commit:** 721a7fd - feat: story-005 - Implement analyzer agent feedback categorization

## Next Stories
(To be populated by planner agent)

## Story 006 ✅ (Completed: 2026-02-16 18:07 EST - RETRY SUCCESSFUL)
**Title:** Implement revisor agent content revision logic

**What was done (initial + retry):**
- Created revisor.ts module in src/lib/doc-revision/
- Implemented applyRevisions() function that:
  - Takes file path and categorized feedback as input
  - Creates revision plan sorted by priority (high → medium → low)
  - Applies revisions while preserving markdown/Word formatting
  - Processes feedback in priority order
  - Returns modified content, change log, and revision plan
- **Markdown revision (initial):**
  - Preserves headings (#, ##, ###), list markers, blockquotes, whitespace
  - Preserves code blocks and inline code
  - Category-specific revision logic (factual errors, missing info, tone, structure)
  - Fallback behavior with [NEEDS VERIFICATION], [TODO: add details], [REVIEW TONE]
- **Word revision (retry - NOW IMPLEMENTED):**
  - Created apply-word-revisions.py Python script using python-docx
  - Implemented applyWordRevisions() TypeScript wrapper
  - Uses python-docx to modify paragraphs while preserving styles
  - Converts revision plan to Python-compatible format
  - Calls Python script via spawn with JSON stdin/stdout
  - Returns path to modified .docx file and change log
  - Proper path resolution (src/ not dist/) for Python scripts
- Change log tracking:
  - Records line/paragraph number, original text, revised text, reason, feedback text
  - Only records actual changes (skips unchanged lines)
- Updated revisor agent SOUL.md with usage instructions and examples
- Wrote comprehensive test suite:
  - **Initial:** 23 markdown revision tests
  - **Retry:** Added 3 Word document revision tests
  - **Total:** 26 revisor tests (plus all other suites = 99 tests)
  - All 99 tests pass ✅
- Typecheck/build passes ✅

**Files created/modified:**
- src/lib/doc-revision/revisor.ts (modified - added Word support)
- src/lib/doc-revision/apply-word-revisions.py (new)
- src/lib/doc-revision/create-test-revision-docx.py (new - test fixture generator)
- tests/doc-revision-revisor.test.ts (modified - added Word tests)
- tests/fixtures/test-revision.docx (new - test fixture)
- workflows/doc-revision/agents/revisor/SOUL.md (updated)

**Technical notes:**
- Revision logic preserves case from original comment text
- Markdown prefix extraction handles headings, lists, quotes
- **Word revision:** Python scripts referenced from src/ (not copied to dist/)
- **Word revision:** Uses spawn with stdin/stdout for JSON communication
- **Word revision:** paragraph.clear() + add_run() + style restoration preserves formatting
- Path resolution: `__dirname + '../../../src/lib/doc-revision/apply-word-revisions.py'`

**Acceptance Criteria Met:**
1. ✅ Revisor agent SOUL.md defines revision approach
2. ✅ Takes analyzer output and original file as input
3. ✅ For markdown: preserves heading levels, lists, code blocks, links
4. ✅ **For Word: uses python-docx to modify paragraphs while preserving styles** ← FIXED IN RETRY
5. ✅ Processes feedback in priority order (high → medium → low)
6. ✅ Creates revision plan before applying changes
7. ✅ Returns modified content with change log
8. ✅ Typecheck passes

**Commits:**
- ab7cca8 - feat: story-006 - Implement revisor agent content revision logic (initial - markdown only)
- 453296b - feat: story-006 - Implement Word document revision support (retry - added Word support)
