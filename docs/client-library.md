# Client Library

The Antfarm client library will be a TypeScript SDK that mirrors the CLI workflow lifecycle commands and surface area. It will expose programmatic primitives for working with the agent pipeline, plus a CLI-friendly interface you can use inside Node.js scripts powered by OpenClaw. Detailed guides, examples, and API references will be published here once the SDK reaches beta readiness.
