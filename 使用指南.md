# Antfarm 使用指南

## 📚 目录
1. [快速开始](#快速开始)
2. [核心概念](#核心概念)
3. [工作流详解](#工作流详解)
4. [常用命令](#常用命令)
5. [实战案例](#实战案例)
6. [故障排查](#故障排查)

---

## 🚀 快速开始

### 前置条件
- ✅ Node.js >= 22
- ✅ OpenClaw v2026.2.9+
- ✅ gh CLI（用于创建PR）
- ✅ Git

### 安装工作流

```bash
# 列出所有可用工作流
node antfarm/dist/cli/cli.js workflow list

# 安装 feature-dev 工作流
node antfarm/dist/cli/cli.js workflow install feature-dev

# 安装所有工作流
node antfarm/dist/cli/cli.js install
```

### 运行第一个任务

```bash
# 运行功能开发工作流
node antfarm/dist/cli/cli.js workflow run feature-dev "添加一个简单的Hello World功能"

# 查看运行状态
node antfarm/dist/cli/cli.js workflow status "Hello World"

# 查看所有运行
node antfarm/dist/cli/cli.js workflow runs
```

---

## 🎯 核心概念

### 1. 工作流（Workflow）
工作流是一系列步骤的集合，每个步骤由一个智能体执行。

**内置工作流**：
- `feature-dev` - 功能开发（7个智能体）
- `bug-fix` - Bug修复（6个智能体）
- `security-audit` - 安全审计（7个智能体）

### 2. 智能体（Agent）
智能体是执行特定任务的AI助手，每个智能体有独立的会话和上下文。

**feature-dev 智能体**：
- `planner` - 规划者：分解任务为用户故事
- `setup` - 设置者：准备开发环境
- `developer` - 开发者：实现功能
- `verifier` - 验证者：验证实现
- `tester` - 测试者：集成测试
- `reviewer` - 审查者：代码审查

### 3. 步骤（Step）
步骤是工作流中的单个任务单元，每个步骤有：
- 输入（input）
- 期望输出（expects）
- 最大重试次数（max_retries）
- 失败处理（on_fail）

### 4. 运行（Run）
运行是工作流的一次执行实例，包含：
- 运行ID
- 任务描述
- 状态（running/completed/failed）
- 步骤进度

---

## 📋 工作流详解

### feature-dev 工作流

```
plan → setup → implement → verify → test → PR → review
```

#### 步骤详解

**1. plan（规划）**
- 智能体：planner
- 任务：将任务分解为用户故事
- 输出：用户故事列表（JSON格式）
- 重试：2次
- 失败：升级到人工

**2. setup（设置）**
- 智能体：setup
- 任务：准备开发环境
- 输出：构建命令、测试命令、基线状态
- 重试：2次
- 失败：升级到人工

**3. implement（实现）**
- 智能体：developer
- 任务：实现每个用户故事
- 类型：循环（loop）
- 验证：每个故事完成后验证
- 重试：2次
- 失败：升级到人工

**4. verify（验证）**
- 智能体：verifier
- 任务：验证每个故事的实现
- 输入：开发者输出
- 输出：验证结果
- 重试：2次
- 失败：重试实现步骤

**5. test（测试）**
- 智能体：tester
- 任务：集成测试和E2E测试
- 输入：所有实现
- 输出：测试结果
- 重试：2次
- 失败：重试实现步骤

**6. pr（创建PR）**
- 智能体：developer
- 任务：创建Pull Request
- 工具：gh CLI
- 输出：PR URL
- 失败：升级到人工

**7. review（审查）**
- 智能体：reviewer
- 任务：审查PR
- 工具：gh CLI
- 输出：审查结果（approved/changes_requested）
- 重试：3次
- 失败：升级到人工

### bug-fix 工作流

```
triage → investigate → setup → fix → verify → PR
```

**步骤详解**：

**1. triage（分类）**
- 智能体：triager
- 任务：复现问题
- 输出：问题复现步骤

**2. investigate（调查）**
- 智能体：investigator
- 任务：找到根因
- 输出：根因分析

**3. setup（设置）**
- 智能体：setup
- 任务：准备修复环境
- 输出：环境配置

**4. fix（修复）**
- 智能体：fixer
- 任务：修复问题
- 输出：修复代码

**5. verify（验证）**
- 智能体：verifier
- 任务：验证修复
- 输出：验证结果

**6. pr（创建PR）**
- 智能体：developer
- 任务：创建PR
- 输出：PR URL

### security-audit 工作流

```
scan → prioritize → setup → fix → verify → test → PR
```

**步骤详解**：

**1. scan（扫描）**
- 智能体：scanner
- 任务：扫描安全漏洞
- 输出：漏洞列表

**2. prioritize（优先级排序）**
- 智能体：prioritizer
- 任务：按严重程度排序
- 输出：排序后的漏洞列表

**3. setup（设置）**
- 智能体：setup
- 任务：准备修复环境
- 输出：环境配置

**4. fix（修复）**
- 智能体：fixer
- 任务：修复漏洞（循环）
- 类型：循环
- 验证：每个漏洞修复后验证

**5. verify（验证）**
- 智能体：verifier
- 任务：验证修复（循环）
- 类型：循环

**6. test（测试）**
- 智能体：tester
- 任务：回归测试
- 输出：测试结果

**7. pr（创建PR）**
- 智能体：developer
- 任务：创建PR
- 输出：PR URL

---

## 🔧 常用命令

### 工作流管理

```bash
# 列出所有工作流
node antfarm/dist/cli/cli.js workflow list

# 安装工作流
node antfarm/dist/cli/cli.js workflow install <workflow-id>

# 卸载工作流
node antfarm/dist/cli/cli.js workflow uninstall <workflow-id>

# 卸载所有工作流
node antfarm/dist/cli/cli.js workflow uninstall --all
```

### 运行管理

```bash
# 运行工作流
node antfarm/dist/cli/cli.js workflow run <workflow-id> "<task>"

# 查看运行状态
node antfarm/dist/cli/cli.js workflow status "<query>"

# 查看所有运行
node antfarm/dist/cli/cli.js workflow runs

# 恢复失败的运行
node antfarm/dist/cli/cli.js workflow resume <run-id>

# 停止运行
node antfarm/dist/cli/cli.js workflow stop <run-id>
```

### 仪表板

```bash
# 启动仪表板
node antfarm/dist/cli/cli.js dashboard

# 停止仪表板
node antfarm/dist/cli/cli.js dashboard stop

# 查看仪表板状态
node antfarm/dist/cli/cli.js dashboard status
```

### 日志

```bash
# 查看最近日志
node antfarm/dist/cli/cli.js logs

# 查看特定运行的日志
node antfarm/dist/cli/cli.js logs <run-id>

# 查看最近N行日志
node antfarm/dist/cli/cli.js logs <lines>
```

### 系统管理

```bash
# 安装所有工作流
node antfarm/dist/cli/cli.js install

# 卸载所有工作流
node antfarm/dist/cli/cli.js uninstall

# 卸载所有工作流（强制）
node antfarm/dist/cli/cli.js uninstall --force

# 更新Antfarm
node antfarm/dist/cli/cli.js update

# 查看版本
node antfarm/dist/cli/cli.js version
```

---

## 💡 实战案例

### 案例1：开发新功能

```bash
# 1. 运行功能开发工作流
node antfarm/dist/cli/cli.js workflow run feature-dev "添加用户认证功能"

# 2. 查看运行状态
node antfarm/dist/cli/cli.js workflow status "用户认证"

# 3. 查看详细进度
node antfarm/dist/cli/cli.js logs <run-id>

# 4. 如果失败，恢复运行
node antfarm/dist/cli/cli.js workflow resume <run-id>
```

**预期输出**：
```
Run: a1fdf573
Workflow: feature-dev
Status: running

Steps:
  [done   ] plan (planner)
  [done   ] setup (setup)
  [running] implement (developer)  Stories: 3/7 done
  [pending] verify (verifier)
  [pending] test (tester)
  [pending] pr (developer)
  [pending] review (reviewer)
```

### 案例2：修复Bug

```bash
# 1. 运行Bug修复工作流
node antfarm/dist/cli/cli.js workflow run bug-fix "登录页面在移动端显示异常"

# 2. 查看运行状态
node antfarm/dist/cli/cli.js workflow status "登录页面"

# 3. 查看日志
node antfarm/dist/cli/cli.js logs <run-id>
```

**预期输出**：
```
Run: b2e4f684
Workflow: bug-fix
Status: running

Steps:
  [done   ] triage (triager)
  [done   ] investigate (investigator)
  [done   ] setup (setup)
  [running] fix (fixer)
  [pending] verify (verifier)
  [pending] pr (developer)
```

### 案例3：安全审计

```bash
# 1. 运行安全审计工作流
node antfarm/dist/cli/cli.js workflow run security-audit "审计整个代码库的安全漏洞"

# 2. 查看运行状态
node antfarm/dist/cli/cli.js workflow status "安全审计"

# 3. 查看日志
node antfarm/dist/cli/cli.js logs <run-id>
```

**预期输出**：
```
Run: c3f5g795
Workflow: security-audit
Status: running

Steps:
  [done   ] scan (scanner)
  [done   ] prioritize (prioritizer)
  [done   ] setup (setup)
  [running] fix (fixer)  Vulnerabilities: 2/5 fixed
  [pending] verify (verifier)
  [pending] test (tester)
  [pending] pr (developer)
```

---

## 🔍 故障排查

### 问题1：cron setup failed

**错误信息**：
```
Cannot start workflow run: cron setup failed. Cannot access cron
```

**解决方案**：
```bash
# 检查OpenClaw版本
openclaw version

# 更新OpenClaw
npm update -g openclaw

# 检查cron工具
openclaw cron list
```

### 问题2：gh CLI not found

**错误信息**：
```
gh: command not found
```

**解决方案**：
```bash
# Windows
winget install --id GitHub.cli

# macOS
brew install gh

# Linux
# 参考 https://github.com/cli/cli#installation

# 验证安装
gh --version

# 登录GitHub
gh auth login
```

### 问题3：工作流安装失败

**错误信息**：
```
Skill "agent-browser" not found for agent "verifier"
```

**解决方案**：
```bash
# 这个警告可以忽略，不影响核心功能
# 如果需要浏览器功能，需要安装agent-browser技能

# 查看已安装技能
npx clawhub list

# 安装agent-browser
npx clawhub install agent-browser
```

### 问题4：运行卡住

**症状**：运行状态一直是 "running"，但进度不更新

**解决方案**：
```bash
# 查看日志
node antfarm/dist/cli/cli.js logs <run-id>

# 查看cron任务
openclaw cron list

# 重启cron任务
node antfarm/dist/cli/cli.js workflow ensure-crons <workflow-id>

# 停止运行
node antfarm/dist/cli/cli.js workflow stop <run-id>

# 恢复运行
node antfarm/dist/cli/cli.js workflow resume <run-id>
```

### 问题5：PR创建失败

**错误信息**：
```
Failed to create PR: gh pr create failed
```

**解决方案**：
```bash
# 检查gh CLI登录状态
gh auth status

# 重新登录
gh auth login

# 检查远程仓库
git remote -v

# 确保分支已推送
git push origin <branch>
```

---

## 📊 监控和调试

### 启动仪表板

```bash
# 启动仪表板（默认端口3333）
node antfarm/dist/cli/cli.js dashboard

# 自定义端口
node antfarm/dist/cli/cli.js dashboard --port 8080
```

仪表板功能：
- 查看所有运行
- 监控步骤进度
- 查看智能体输出
- 实时日志查看

### 查看日志

```bash
# 查看最近日志
node antfarm/dist/cli/cli.js logs

# 查看特定运行的日志
node antfarm/dist/cli/cli.js logs <run-id>

# 查看最近100行日志
node antfarm/dist/cli/cli.js logs 100
```

### 调试模式

```bash
# 查看详细日志
# 在工作流配置中设置debug: true

# 查看智能体会话
# 使用sessions_list和sessions_history工具
```

---

## 🎯 最佳实践

### 1. 任务描述要清晰
```bash
# ❌ 不好的描述
node antfarm/dist/cli/cli.js workflow run feature-dev "添加功能"

# ✅ 好的描述
node antfarm/dist/cli/cli.js workflow run feature-dev "添加用户认证功能，支持GitHub OAuth和邮箱密码登录"
```

### 2. 定期检查运行状态
```bash
# 每隔一段时间检查状态
node antfarm/dist/cli/cli.js workflow status "<task>"

# 查看日志了解详情
node antfarm/dist/cli/cli.js logs <run-id>
```

### 3. 及时处理失败
```bash
# 如果运行失败，查看日志
node antfarm/dist/cli/cli.js logs <run-id>

# 恢复运行
node antfarm/dist/cli/cli.js workflow resume <run-id>
```

### 4. 使用仪表板监控
```bash
# 启动仪表板
node antfarm/dist/cli/cli.js dashboard

# 在浏览器中打开 http://localhost:3333
```

### 5. 定期更新
```bash
# 更新Antfarm
node antfarm/dist/cli/cli.js update

# 更新OpenClaw
npm update -g openclaw
```

---

## 📚 更多资源

- [Antfarm GitHub](https://github.com/snarktank/antfarm)
- [Antfarm 文档](https://github.com/snarktank/antfarm#readme)
- [OpenClaw 文档](https://docs.openclaw.ai)
- [Ralph 循环](https://github.com/snarktank/ralph)

---

## 🆘 获取帮助

```bash
# 查看帮助
node antfarm/dist/cli/cli.js --help

# 查看特定命令的帮助
node antfarm/dist/cli/cli.js workflow --help
node antfarm/dist/cli/cli.js workflow run --help
```

---

**祝使用愉快！** 🚀
